# 🌐 Application Identity
spring.application.name=hospital-management

# 🛠️ Server Configuration
server.port=9999

# 🗄️ Database Configuration
spring.datasource.url=jdbc:mysql://mysql:3306/hospitalu?useSSL=false&serverTimezone=UTC  # ✅ CHANGED localhost → mysql
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# 🧠 JPA & Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# 🔐 JWT Configuration
jwt.secret=Gowtham@Hospital2025!SecureKey123456789
jwt.expiration=86400000

# 🌍 CORS
cors.allowed-origins=http://localhost:30082  # ✅ CHANGED to match frontend NodePort

# 📦 Logging
logging.level.org.springframework=INFO
logging.level.com.hospital=DEBUG
